
<div class="login_form_box">
  <h2>新規登録</h2>
  <p>お客様情報を入力してください。<br>
    入力が終わりましたら次へをクリックしてください。<br>
  ※は必須項目です。</p>

  <div class="row">
    <div class="col-md-3"></div>
    <div class="col-md-6">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= devise_error_messages! %>

        <div class="field">
          ※メールアドレス：<%= f.email_field :email, autofocus: true, autocomplete: "email" %>
        </div>

        <div class="field">
          ※苗字：<%= f.text_field :last_name, autofocus: true, autocomplete: "off" %>
        </div>

        <div class="field">
          ※苗字フリガナ：<%= f.text_field :last_name_kana, autofocus: true, autocomplete: "off" %>
        </div>

        <div class="field">
          ※名前：<%= f.text_field :first_name, autofocus: true, autocomplete: "off" %>
        </div>


        <div class="field">
          ※名前フリガナ：<%= f.text_field :first_name_kana, autofocus: true, autocomplete: "off" %>
        </div>

        <div class="field">
          ※郵便番号：<%= f.text_field :postal_code, autofocus: true, autocomplete: "off" %>
        </div>

        <div class="field">
          ※住所：<%= f.text_field :address, autofocus: true, autocomplete: "off" %>
        </div>

        <div class="field">
          ※電話番号：<%= f.text_field :tel_num, autofocus: true, autocomplete: "off" %>
        </div>

        <div class="field">
          <% if @minimum_password_length %>
          <em><!-- (<%#= @minimum_password_length %> characters minimum) --></em>
          <% end %><br />
          ※パスワード：<%= f.password_field :password, autocomplete: "off" %>
        </div><br>

        <div class="field">
          ※パスワード(確認)：<%= f.password_field :password_confirmation, autocomplete: "off" %>
        </div>

        <div class="actions">
          <%= f.submit "Sign up" %>
        </div>
      <% end %>
      <br>
      <p>
        <%= render "devise/shared/links" %>
      </p>
      </div>
      <div class="col-md-3"></div>
  </div>
</div>




